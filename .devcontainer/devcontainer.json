{
  "name": "webpage (Quarto)",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "postCreateCommand": "curl -fsSL -o /tmp/quarto.deb https://github.com/quarto-dev/quarto-cli/releases/download/v1.10.18/quarto-1.10.18-linux-amd64.deb && sudo apt-get update -y && sudo apt-get install -y /tmp/quarto.deb && rm /tmp/quarto.deb && curl -LsSf https://astral.sh/uv/install.sh | sh && export PATH=\"$HOME/.local/bin:$PATH\" && uv sync && uv run playwright install --with-deps chromium",
  "customizations": {
    "vscode": {
      "extensions": ["quarto.quarto"]
    }
  },
  "forwardPorts": [4848],
  "portsAttributes": {
    "4848": {
      "label": "Quarto preview",
      "onAutoForward": "openBrowser"
    }
  }
}
